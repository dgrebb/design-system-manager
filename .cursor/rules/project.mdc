# Theme Lab POC - Project Rules

## Project Overview

This is a Theme Lab POC that enables:
- Live tweaking of global design tokens with real-time Storybook updates
- Per-component local overrides
- Multi-framework export (tokens + web components)
- Storybook Composition for "base UI libraries" integration

## Toolchain

- **Runtime**: Bun (workspace, installs, scripts, tasks)
- **Language**: TypeScript everywhere
- **Structure**: Monorepo with `/packages` and `/apps`
- **Components**: Web Components (Lit, light DOM preferred)
- **Preview**: Storybook (Web Components renderer)
- **Tokens**: CSS variables at runtime

## Key Conventions

### Tokens Pipeline
- `tokens.json` is the canonical source
- Generated outputs:
  - `tokens.css` (CSS custom properties)
  - `tokens.dark.css` (dark theme variant)
  - `tailwind.theme.css` (Tailwind v4 CSS-first theme)
- Web components consume tokens via CSS variables only

### Web Components
- Implement with Lit (light DOM first)
- Shadow DOM is optional, behind a flag if used
- Target components: Button, Input, Checkbox, Switch, Select, Card, Dialog/Modal, Tooltip, Tabs

### Storybook
- Primary: Web Components Storybook ("Theme Lab")
- Custom addon panel for token editing
- Preset save/load via localStorage
- Export buttons for tokens + CSS outputs

## Branch Naming

```
poc/m0-foundation
poc/m1-tokens
poc/m2-wc-components
poc/m3-theme-lab-addon
poc/m4-storybook-hub
poc/m5-framework-examples
```

## Commit Messages

Reference milestone tasks:
- `M0: initialize bun workspace`
- `M1: add token schema and generators`
- `M2: implement Button component`
- `M3: storybook addon panel for token editing`

## Required Files

Always maintain:
- `TODO.md` — canonical checklist by milestone
- `WORKLOG.md` — append-only engineering log

## Code Style

- Prefer functional patterns
- Use TypeScript strict mode
- Follow Lit best practices for web components
- Accessibility first: focus states, keyboard interactions
